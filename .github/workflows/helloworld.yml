on: workflow_dispatch
jobs:
  print-helloworld:
    runs-on: ubuntu-latest
    steps:
      - run: |
          apt update
      - run: |
          sudo apt update
      - run: |
          curl google.com
